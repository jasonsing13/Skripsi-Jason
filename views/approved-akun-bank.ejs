<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head.ejs') %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Approved Akun Bank</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <%- include('partials/style.ejs') %> 
    <style>
        body {
            padding: 20px;
        }
        .header {
            margin-bottom: 30px;
            text-align: center;
        }
        .container {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
        }
        .alert {
            color: #856404;
            background-color: #fff3cd;
            border-color: #ffeeba;
        }
    </style>
</head>
<body>
    <%- include('partials/sidebar.ejs') %>
    <div class="container mt-4">
        <h1>Vendor Profile</h1>
        <ul class="nav nav-tabs">
            <li class="nav-item">
                <a class="nav-link" href="/approved-vendor-profile?id=<%= vendor_id %>">Informasi Vendor</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="/approved-akun-bank?id=<%= vendor_id %>">Akun Bank</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/approved-perpajakan?id=<%= vendor_id %>">Perpajakan</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/approved-legalitas?id=<%= vendor_id %>">Legalitas</a>
            </li>
        </ul>
        <h3><%= vendor.nama_vendor %></h3>
        <p>Status Verifikasi: <span class="badge bg-success">Data Sudah Lengkap</span></p>
        <div class="row">
            <div class="col-md-6">
                <p>Nomor Rekening: <%= vendor.no_rekening %></p>
            <p>Nama Pemilik Rekening: <%= vendor.nama_pemilik_rekening %></p>
                <p>Nama Bank: <%= vendor.nama_bank %></p>
            </div>
        </div>
        <button class="btn btn-primary">Update</button>
    </div>
</body>
</html>