<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head.ejs') %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profil Informasi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <%- include('partials/style.ejs') %> 
    <style>
        body {
            padding: 20px;
        }
        .header {
            margin-bottom: 30px;
            text-align: center;
        }
        .container {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
        }
        .alert {
            color: #856404;
            background-color: #fff3cd;
            border-color: #ffeeba;
        }
    </style>
</head>
<body>
    <%- include('partials/sidebar.ejs') %>
    <div class="container mt-4">
        <h1>Vendor Profile</h1>
        <ul class="nav nav-tabs">
            <li class="nav-item">
              <a class="nav-link active" href="/profil-informasi?id=<%= vendor_id %>">Informasi Vendor</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/profil-bank?id=<%= vendor_id %>">Akun Bank</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/profil-perpajakan?id=<%= vendor_id %>">Perpajakan</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/profil-legalitas?id=<%= vendor_id %>">Legalitas</a>
            </li>
          </ul>
          <div class="col-md-9">
                <!-- Main Content -->
                <div class="alert">
                    <a href="/upload-dokumen-vendor?id=<%= vendor_id %>" class="btn btn-primary">Lengkapi Dokumen Vendor</a>                  
                    <p>Segera lengkapi dokumen vendor untuk bisa bergabung dalam pengadaan MPM</p>
                </div>
                <h3>PT. Setia Berkah Abadi</h3>
                <p>Status Verifikasi: <span class="badge bg-success">Data Belum Lengkap</span></p>
                <div class="row">
                    <div class="col-md-6">
                        <h4>Informasi Vendor</h4>
                        <p>Kode Vendor: <%= vendor.vendor_id %></p>
                        <p>Nama Vendor: <%= vendor.nama_vendor %></p>
                        <p>Status Kantor: <%= vendor.status_kantor %></p>
                        <p>Nama Direktur/Pemilik: <%= vendor.nama_direktur %></p>
                        <p>Nomor Telepon Perusahaan: <%= vendor.no_telp %></p>
                    </div>
                    <div class="col-md-6">
                        <h4>Email Perusahaan</h4>
                        <p>Email Perusahaan: <%= vendor.email_perusahaan %></p>
                        <h4>Alamat Perusahaan</h4>
                        <p>Alamat Perusahaan: <%= vendor.alamat_perusahaan %></p>
                        <p>Negara: <%= vendor.negara %></p>
                        <p>Provinsi: <%= vendor.nama_provinsi %></p>
                        <p>Kabupaten/Kota: <%= vendor.nama_kk %></p>
                    </div>
                </div>
                <button class="btn btn-primary">Update</button>
            </div>
        </div>
    </div>
</body>
</html>